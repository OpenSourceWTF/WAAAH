# WAAAH Global Configuration

# Server Configuration
WAAAH_PORT=3000
MCP_SERVER_URL=http://localhost:3000
# For production (Docker), use:
# MCP_SERVER_URL=https://yourdomain.com

# API Key for server authentication (shared secret)
# Generate with: openssl rand -hex 32
WAAAH_API_KEY=

# Agent Configuration
AGENTS_CONFIG=./config/agents.yaml

# Database
# Local: ./data/waaah.db
# Docker: /app/data/waaah.db
DB_PATH=./data/waaah.db

# Discord Bot
DISCORD_TOKEN=
# Comma-separated list of Discord User IDs allowed to use the bot
APPROVED_USERS=
# Channel ID for posting delegation logs
DELEGATION_CHANNEL_ID=

# Production Deployment (Docker + Certbot)
DOMAIN_NAME=example.com
