# ==========================================
# Dashboard Client Environment Configuration
# ==========================================
# Copy this to .env.local and fill in your API key

# API Key - REQUIRED for authenticated endpoints
# Get the API key from ONE of these sources (in priority order):
#   1. WAAAH_API_KEY in root .env
#   2. ~/.waaah/credentials.json (auto-generated by server on first run)
#
# Example: VITE_WAAAH_API_KEY=waaah-abc123...
VITE_WAAAH_API_KEY=

# Server URL (leave empty to use relative URLs, which works with built-in proxy)
# Only needed if dashboard is served from a different domain than the API
# VITE_WAAAH_SERVER_URL=http://localhost:3000
